<div class="container">

        <!-- Title -->
        <div class="row">
            <div class="col-lg-12">
                <h3>Nos logements</h3>
            </div>
        </div>
        <!-- /.row -->

        <!-- Page Features -->
        <div class="row text-center">
          <% @logements.each do |logement| %> <!--link_to "galerie photo" , :id -> logement.id-->   
            <div class="col-md-3 col-sm-6 hero-feature">
                <div class="thumbnail">
                    <img src="<%= logement.image.url %>" alt="">
                    <div class="caption">
                        <h3><%= logement.name %> </h3>
                        
                        <p>
                            <!--<a href="#" class="btn btn-primary"><%= logement.price %> €</a> <a href="galerie.html" class="btn btn-default">Plus d'infos</a>--><%= link_to "galerie photo" ,galerie_path(:id => logement.id) %>
                            
                        </p>
                    </div>
                </div>
            </div>
          <% end %>
            
        </div>
        <hr>

       
        

</div>
    <!-- /.container -->
