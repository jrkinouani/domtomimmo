<div class="container">

        <!-- Title -->
        <div class="row">
            <div class="col-lg-12">
                <h3>Nos logements</h3>
            </div>
        </div>
        <!-- /.row -->

        <!-- Page Features -->
        <div class="row text-center">
          <% @logements.each do |logement| %> <!--link_to "galerie photo" , :id -> logement.id-->   
            <div class="col-md-3 col-sm-6 hero-feature">
                <div class="thumbnail">
                    <img src="<%= logement.image.url %>" alt="">
                    <div class="caption">
                        <h4 class="desc_home"><%= logement.desc_home %> </h4>
                        
                        <p>
                            <a href="#" class="btn btn-primary"><%= logement.price %> €</a> <%= link_to "Plus d'infos" ,galerie_path(:id => logement.id), :class => "price" %>
                            
                        </p>
                    </div>
                </div>
            </div>
          <% end %>
            
        </div>
        <hr>

       <!--/moteur -->

            <!--<% unless (controller_path == 'static_pages' && action_name == 'home') %>-->

             <%= form_tag(search_path, method: :get, id: "search-form", class: "navbar-form navbar-right", role: "search" ) do  %>
            <div  id="search" >
                <%= text_field_tag 'city', nil, placeholder: "Rechercher un logement",  type: "text", class:"form-control"%>
            </div>
              
         <% end %>
         
      <!--<% end %>-->

       <!--fin moteur -->
        

</div>
    <!-- /.container -->
